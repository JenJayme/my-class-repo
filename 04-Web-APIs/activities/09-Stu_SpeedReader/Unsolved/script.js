var poem = "Some say the world will end in 🔥, Some say in ice. From what I’ve tasted of desire, I hold with those who favor fire. But if it had to perish twice, I think I know enough of hate. To say that for destruction ice, Is also great, And would suffice.";

//create pointers to word and countdown elements

var countDown 
var word
var poemArray = [poem]

function prepareRead() {
  // Create the countdown timer.  Use setTimeout and setInterval to show the timer
  // Split the poem into a list of words. 
  // setInterval (function() {alert ("Hello"); }, 3000);
  // var x = function (a, b) {return a * b};
  // we're deciding to create a function rather use the anonymous function method.
  // define functions for countdown 
}

function speedRead() {
  // To be triggered after countdown timer function 
  // Print words to the screen one at a time.  Use setInterval to read a word every second. To show the word, write to var word
}

// call prepareRead 
// call speedRead